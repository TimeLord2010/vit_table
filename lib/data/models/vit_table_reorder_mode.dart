enum VitTableReorderMode {
  /// The entire row is the drag target.
  row,

  /// A drag handle icon is placed at the leading (left) edge of each row.
  leading,

  /// A drag handle icon is placed at the trailing (right) edge of each row.
  trailing,
}
